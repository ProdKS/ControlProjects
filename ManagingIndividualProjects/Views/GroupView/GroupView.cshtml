@model List<Group>
@{
    ViewData["Title"] = "Админ страница(Группы)";
}
<body>
    <h1 class="title">Группы</h1>
    <div class="ContainerDeprtaments">
        @foreach (var groups in Model)
        {
            <div>
                <a href="@Url.Action("GroupDetailView", "GroupView", new {groupsId = groups.Id})">@groups.Name</a>
            </div>
        }
    </div>
</body>